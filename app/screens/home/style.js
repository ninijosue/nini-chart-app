import { StyleSheet } from "react-native";
import { customHeight, customWidth } from "../../helper/screen";

export default StyleSheet.create(
    {
        logoContiner: {
            paddingTop: customHeight(5),
            paddingLeft: customWidth(5)
        },
        
    }
)